To open and use this demo, make sure to install the component TMyPlanner first in the IDE.

To do this, start a new package from the IDE, add MyPlanner.pas to this package file and then compile & install this package in the IDE.

After this step, TMyPlanner will be added to the tool palette and you can open the demo.